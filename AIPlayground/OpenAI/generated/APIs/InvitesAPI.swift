//
// InvitesAPI.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

enum HTTPMethod: String {
    case get = "GET"
    case post = "POST"
    case put = "PUT"
    case delete = "DELETE"
    case head = "HEAD"
    case patch = "PATCH"
    case options = "OPTIONS"
    case trace = "TRACE"
    case connect = "CONNECT"

    init(stringValue: String) {
        guard let method = HTTPMethod(rawValue: stringValue.uppercased()) else {
            self = .get
            return
        }
        self = method
    }
}

internal struct Request {
    var url: URL
    var method: HTTPMethod
    var body: Data? = nil
    var queryParams: [URLQueryItem] = []
    var headers: [String: String] = [:]

    func urlRequest() throws -> URLRequest {
        var urlComponents = URLComponents(url: url, resolvingAgainstBaseURL: true)!
        var existingQueryItems = urlComponents.queryItems ?? []
        existingQueryItems.append(contentsOf: queryParams)
        urlComponents.queryItems = existingQueryItems
        var urlRequest = URLRequest(url: urlComponents.url!)
        headers.forEach { (k, v) in
            urlRequest.setValue(v, forHTTPHeaderField: k)
        }
        urlRequest.httpMethod = method.rawValue
        urlRequest.httpBody = body
        return urlRequest
    }
}

protocol InvitesAPITransport: Sendable {
    func execute(request: Request) async throws -> (Data, URLResponse)
}

protocol InvitesAPIClientMiddleware: Sendable {
    func intercept(
        _ request: Request,
        next: (Request) async throws -> (Data, URLResponse)
    ) async throws -> (Data, URLResponse)
}


open class InvitesAPI: InvitesAPIEndpoints, @unchecked Sendable {
    internal var transport: InvitesAPITransport
    internal var middlewares: [InvitesAPIClientMiddleware]
    internal var basePath: String
    internal var jsonDecoder: JSONDecoder

    init(basePath: String, transport: InvitesAPITransport, middlewares: [InvitesAPIClientMiddleware], jsonDecoder: JSONDecoder = JSONDecoder.default) {
        self.basePath = basePath
        self.transport = transport
        self.middlewares = middlewares
        self.jsonDecoder = jsonDecoder
    }

    func send<Response: Codable>(
        request: Request,
        deserializer: (Data) throws -> Response
    ) async throws -> Response {

        // TODO: make this a bit nicer and create an API error to make it easier to handle stuff
        func makeError(_ error: Error) -> Error {
            return error
        }

        func wrappingErrors<R>(
            work: () async throws -> R,
            mapError: (Error) -> Error
        ) async throws -> R {
            do {
                return try await work()
            } catch {
                throw mapError(error)
            }
        }

        let (data, _) = try await wrappingErrors {
            var next: (Request) async throws -> (Data, URLResponse) = { _request in
                try await wrappingErrors {
                    try await self.transport.execute(request: _request)
                } mapError: { error in
                    makeError(error)
                }
            }
            for middleware in middlewares.reversed() {
                let tmp = next
                next = {
                    try await middleware.intercept(
                        $0,
                        next: tmp
                    )
                }
            }
            return try await next(request)
        } mapError: { error in
            makeError(error)
        }

        return try await wrappingErrors {
            try deserializer(data)
        } mapError: { error in
            makeError(error)
        }
    }

    func makeRequest(
        uriPath: String,
        queryParams: [URLQueryItem] = [],
        httpMethod: String
    ) throws -> Request {
        let url = URL(string: basePath + uriPath)!
        return Request(
            url: url,
            method: .init(stringValue: httpMethod),
            queryParams: queryParams,
            headers: ["Content-Type": "application/json"]
        )
    }

    func makeRequest<T: Encodable>(
        uriPath: String,
        queryParams: [URLQueryItem] = [],
        httpMethod: String,
        request: T
    ) throws -> Request {
        var r = try makeRequest(uriPath: uriPath, queryParams: queryParams, httpMethod: httpMethod)
        r.body = try JSONEncoder().encode(request)
        return r
    }


    /**
     Delete an invite. If the invite has already been accepted, it cannot be deleted.
     
     - parameter inviteId: (path) The ID of the invite to delete. 
     - returns: InviteDeleteResponse
     */

    open func deleteInvite(inviteId: String) async throws -> InviteDeleteResponse {
        var localVariablePath = "/organization/invites/{invite_id}"
        let inviteIdPreEscape = "\(APIHelper.mapValueToPathItem(inviteId))"
        let inviteIdPostEscape = inviteIdPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        localVariablePath = localVariablePath.replacingOccurrences(of: "{invite_id}", with: inviteIdPostEscape, options: .literal, range: nil)
        
        let urlRequest = try makeRequest(
            uriPath: localVariablePath,
            httpMethod: "DELETE"
        )
        return try await send(request: urlRequest) {
            try self.jsonDecoder.decode(InviteDeleteResponse.self, from: $0)
        }
    }
    /**
     Delete an invite. If the invite has already been accepted, it cannot be deleted.
     - DELETE /organization/invites/{invite_id}
     - parameter inviteId: (path) The ID of the invite to delete. 
     - returns: RequestBuilder<InviteDeleteResponse> 
     */


    /**
     Create an invite for a user to the organization. The invite must be accepted by the user before they have access to the organization.
     
     - parameter inviteRequest: (body) The invite request payload. 
     - returns: Invite
     */

    open func inviteUser(inviteRequest: InviteRequest) async throws -> Invite {
        let localVariablePath = "/organization/invites"
        
        let urlRequest = try makeRequest(
            uriPath: localVariablePath,
            httpMethod: "POST",
            request: inviteRequest
        )
        return try await send(request: urlRequest) {
            try self.jsonDecoder.decode(Invite.self, from: $0)
        }
    }
    /**
     Create an invite for a user to the organization. The invite must be accepted by the user before they have access to the organization.
     - POST /organization/invites
     - parameter inviteRequest: (body) The invite request payload. 
     - returns: RequestBuilder<Invite> 
     */


    /**
     Returns a list of invites in the organization.
     
     - parameter limit: (query) A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.  (optional, default to 20)
     - parameter after: (query) A cursor for use in pagination. &#x60;after&#x60; is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after&#x3D;obj_foo in order to fetch the next page of the list.  (optional)
     - returns: InviteListResponse
     */

    open func listInvites(limit: Int? = nil, after: String? = nil) async throws -> InviteListResponse {
        let localVariablePath = "/organization/invites"
        let queryParams = APIHelper.mapValuesToQueryItems([
            "limit": (wrappedValue: limit?.encodeToJSON(), isExplode: true),
            "after": (wrappedValue: after?.encodeToJSON(), isExplode: true),
        ])
        let urlRequest = try makeRequest(
            uriPath: localVariablePath,
            queryParams: queryParams ?? [],
            httpMethod: "GET"
        )
        return try await send(request: urlRequest) {
            try self.jsonDecoder.decode(InviteListResponse.self, from: $0)
        }
    }
    /**
     Returns a list of invites in the organization.
     - GET /organization/invites
     - parameter limit: (query) A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.  (optional, default to 20)
     - parameter after: (query) A cursor for use in pagination. &#x60;after&#x60; is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after&#x3D;obj_foo in order to fetch the next page of the list.  (optional)
     - returns: RequestBuilder<InviteListResponse> 
     */


    /**
     Retrieves an invite.
     
     - parameter inviteId: (path) The ID of the invite to retrieve. 
     - returns: Invite
     */

    open func retrieveInvite(inviteId: String) async throws -> Invite {
        var localVariablePath = "/organization/invites/{invite_id}"
        let inviteIdPreEscape = "\(APIHelper.mapValueToPathItem(inviteId))"
        let inviteIdPostEscape = inviteIdPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        localVariablePath = localVariablePath.replacingOccurrences(of: "{invite_id}", with: inviteIdPostEscape, options: .literal, range: nil)
        
        let urlRequest = try makeRequest(
            uriPath: localVariablePath,
            httpMethod: "GET"
        )
        return try await send(request: urlRequest) {
            try self.jsonDecoder.decode(Invite.self, from: $0)
        }
    }
    /**
     Retrieves an invite.
     - GET /organization/invites/{invite_id}
     - parameter inviteId: (path) The ID of the invite to retrieve. 
     - returns: RequestBuilder<Invite> 
     */

}

protocol InvitesAPIEndpoints {


        func deleteInvite(inviteId: String) async throws -> InviteDeleteResponse


        func inviteUser(inviteRequest: InviteRequest) async throws -> Invite


        func listInvites(limit: Int?, after: String?) async throws -> InviteListResponse


        func retrieveInvite(inviteId: String) async throws -> Invite


}

